/**
 * Logs errors that occur when applying variables to Figma nodes
 * @param error The error that occurred
 */
export function logApplyVariableError(error: unknown): void {
  console.log('error', error);
}
