import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import Stack from '@/app/components/Stack';

<Meta title='Stack' component={Stack}/>

<ArgsTable of={Stack} />

export const StackDefaultStory = (args) => {
  return (
    <Stack gap={args.gap} direction={args.direction} align={args.align} justify={args.justify} width={args.width}>
      <div style={{ backgroundColor: '#000', width: '16px', height: '16px' }} />
      <div style={{ backgroundColor: '#000', width: '16px', height: '16px' }} />
      <div style={{ backgroundColor: '#000', width: '16px', height: '16px' }} />
    </Stack>
  )
}

<Canvas>
  <Story name='Primary' args={{
    gap: 1,
    direction: 'column',
    align: 'start',
    justify: 'start',
    wrap: false,
  }}>
    {StackDefaultStory.bind({})}
  </Story>
</Canvas>