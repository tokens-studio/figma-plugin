import { Flex } from '../Flex';
import { styled } from '@/stitches.config';

export const StyledBeforeFlex = styled(Flex, {
  paddingRight: '$2',
  gap: '0px',
  alignSelf: 'stretch',
  alignItems: 'center',
});
