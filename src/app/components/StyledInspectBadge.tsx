import { styled } from '@/stitches.config';

export const StyledInspectBadge = styled('div', {
  padding: '$2',
  color: '$text',
  background: '$bgSubtle',
  borderRadius: '$default',
  fontSize: '$xsmall',
});
