import { styled } from '@/stitches.config';

export const StyledItemValue = styled('div', {
  flexShrink: 0,
  color: '$textMuted',
  fontWeight: '$bold',
  textAlign: 'right',
  textTransform: 'uppercase',
});
