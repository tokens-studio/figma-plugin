import { Meta, Story, ArgsTable, Canvas, Preview } from "@storybook/addon-docs";
import { Provider } from 'react-redux';

import { store } from '@/app/store';
import { SingleToken } from '@/types/tokens';
import { TokenTypes } from '@/constants/TokenTypes';

import { TokenButton } from '@/app/components/TokenButton';
import TokenTree from '@/app/components/TokenTree';

<Meta 
	title="TokenButton"
	component={TokenButton}
	args={{
		type: TokenTypes.SIZING,
		displayType:'GRID',
		draggedToken: SingleToken | null,
  	dragOverToken: SingleToken | null,
	}}
/>

<ArgsTable of={TokenButton}/>

export const TokenButtonDefaultStory = (args) => {
	return (
		<Provider store={store}>
			<TokenButton {...args}/>
		</Provider>
	)
}

<Canvas>
	<Story name="TokenButton" args={{
	}}>
		{TokenButtonDefaultStory.bind({})}
	</Story>
</Canvas>