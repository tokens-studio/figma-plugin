import { useState } from 'react';
import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import { Provider } from 'react-redux';

import { store } from '@/app/store';

import EditTokenFormModal from '@/app/components/EditTokenFormModal';
import Button from '@/app/components/Button/Button';

<Meta 
  title='EditTokenFormModal' 
  component={EditTokenFormModal}
  args={{
    resolvedTokens:[
      {
        value: 2,
        type: 'sizing',
        name: '2',
        internal__Parent: 'core',
        rawValue: '2'
      },
      {
        value: 11,
        type: 'sizing',
        name: '1111',
        internal__Parent: 'core',
        rawValue: '11'
      },
      {
        value: '#f8fafc',
        type: 'color',
        name: 'color.slate.50',
        internal__Parent: 'core',
        rawValue: '#f8fafc'
      }
    ]
  }}
/>

<ArgsTable of={EditTokenFormModal}/>

export const EditTokenFormModalDefaultStory = (args) => {
  const [isOpen, setIsOpen] = useState(false);
  return (
    <>
    <Button onClick={() => setIsOpen(true)}>
      Click to Open a EditTokenFormModal Modal
    </Button>
    <Provider store={store}>
      {isOpen && <EditTokenFormModal {...args} isOpen={isOpen}/>}
    </Provider>
    </>
  )
}

<Canvas>
  <Story name='Default'>
    {EditTokenFormModalDefaultStory.bind({})}
  </Story>
</Canvas>