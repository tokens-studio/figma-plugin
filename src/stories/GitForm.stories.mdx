import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';

import GitForm from '@/app/components/StorageItemForm/GitForm';

<Meta 
  title='GitForm' 
  component={GitForm} 
  args={{
    values: {
      secret: '',
      id: '',
      branch: '',
      filePath: '',
      baseUrl: '',
    }
  }}
/>

<ArgsTable of={GitForm}/>

export const GitFormDefaultStory = (args) => {
  return (
    <>
    <GitForm values={args.values}/>
    </>
  )
}

<Canvas>
  <Story name='Default'>
    {GitFormDefaultStory.bind({})}
  </Story>
</Canvas>