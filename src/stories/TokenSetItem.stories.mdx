import { useState } from 'react';
import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import { Provider } from 'react-redux';

import { store } from '@/app/store';
import { TokenSetItem } from '@/app/components/TokenSetItem';

<Meta 
  title='TokenSetItem' 
  component={TokenSetItem}
  args={{
    item: {
      isLeaf: true,
      path: 'global',
      key: 'global-set',
      parent: null,
      level: 0,
      label: 'global',
      tokenSets: [
        'global'
      ]
    },
    isActive: false,
    isChecked: false,
    canEdit: true,
    canDelete: true,
    canReorder: false
  }}
/>

<ArgsTable of={TokenSetItem}/>

export const TokenSetItemDefaultStory = (args) => {
  const [isChecked, setIsChecked] = useState(false);
  return (
    <Provider store={store}>
      <TokenSetItem {...args} isChecked={isChecked} onCheck={() => setIsChecked(!isChecked)}/>
    </Provider>
  )
}

<Canvas>
  <Story name='Default'>
    {TokenSetItemDefaultStory.bind({})}
  </Story>
</Canvas>