import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import { Provider } from 'react-redux';

import { store } from '@/app/store';

import TokensBottomBar from '@/app/components/TokensBottomBar';
<Meta 
  title='TokensBottomBar'
  component={TokensBottomBar}
/>

<ArgsTable of={TokensBottomBar} />

export const TokensBottomBarDefaultStory = (args) => {
  return (
    <Provider store={store}>
      <TokensBottomBar {...args} />
    </Provider>
  )
}

<Canvas>
  <Story name='Primary' args={{
    label: 'Primary',
    path: 'default.token',
    id: 'listing',
  }}>
    {TokensBottomBarDefaultStory.bind({})}
  </Story>
</Canvas>