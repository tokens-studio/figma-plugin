import { useState } from 'react';
import { Meta, Story, ArgsTable, Canvas, Preview } from '@storybook/addon-docs';
import { Provider } from 'react-redux';

import { store } from '@/app/store';
import TokenSetTree from '@/app/components/TokenSetTree';

<Meta 
  title='TokenSetTree' 
  component={TokenSetTree}
  args={{
    tokenSets: [
      'core',
      'semantic',
      'button',
      'icon-button',
      'link',
      'text-field',
      'radio',
      'checkbox',
      'switch',
      'field-label',
      'help-text',
      'heading',
      'body',
      'detail',
      'code',
      'avatar',
      'tag',
      'alert-banner',
      'divider',
      'menu-item'
    ]
  }}
/>

<ArgsTable of={TokenSetTree}/>

export const TokenSetTreeDefaultStory = (args) => {
  return (
    <Provider store={store}>
      <TokenSetTree {...args} />
    </Provider>
  )
}

<Canvas>
  <Story name='Default'>
    {TokenSetTreeDefaultStory.bind({})}
  </Story>
</Canvas>