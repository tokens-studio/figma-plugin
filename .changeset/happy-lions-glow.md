---
"@tokens-studio/figma-plugin": patch
---

Add UI controls for theme inheritance. Users can now select a parent theme from the "Extends theme" dropdown when editing a theme. The theme list displays inheritance relationships with "extends ParentName" indicators.
