---
"@tokens-studio/figma-plugin": patch
---

Color token modifiers are not available to Studio users, so the modifier section is now hidden when the active storage provider is Studio. Instead, Studio users would use TokenScript, read more at https://documentation-v2.tokens.studio/getting-started/glossary.html#schema-defined-function
