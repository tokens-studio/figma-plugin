---
"@tokens-studio/figma-plugin": patch
---

Change default token format from Legacy to DTCG format. New users will now use DTCG format ($value, $type) by default instead of the legacy format (value, type).
