---
"@tokens-studio/figma-plugin": patch
---

implement two-way sync between Figma plugin and Studio platform — theme mutations (create, update, delete, style/variable assignments, disconnects, renames) are now automatically pushed to the Studio REST API in real time
