---
"@tokens-studio/figma-plugin": patch
---

Fixed an issue where duplicate Tokens Studio OAuth providers were created and persisted in the storage when switching branches, causing visual and functional duplication in the Sync Settings.
