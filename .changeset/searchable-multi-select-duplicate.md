---
"@tokens-studio/figma-plugin": minor
---

Duplicating a token or a token group no longer closes the set picker after every click. The picker is now a proper multi-select panel that stays open while you tick sets, has a search field at the top to filter long set lists, and a select-all toggle that acts on whatever the search currently shows.
