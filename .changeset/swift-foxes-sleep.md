---
"@tokens-studio/figma-plugin": patch
---

Add support for updating variables in extended collections. Override values can now be set on inherited variables, and inherited variables are protected from deletion when syncing to Figma.
