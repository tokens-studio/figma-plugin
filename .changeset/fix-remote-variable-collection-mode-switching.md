---
"@tokens-studio/figma-plugin": patch
---

Fix unable to switch variable modes in non-local (external library) setup. The plugin now imports remote variable collections when needed to enable mode switching for variables from published libraries.
