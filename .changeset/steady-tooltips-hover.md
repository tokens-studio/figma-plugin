---
"@tokens-studio/figma-plugin": patch
---

Fixed token value tooltips appearing late or not at all when hovering tokens in large token sets. Hovering a token no longer re-renders every other token in the list, and neither does changing the selection or applying a token.
