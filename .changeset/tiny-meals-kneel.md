---
"@tokens-studio/figma-plugin": patch
---

Added support for motion, duration, and cubicBezier token types. Duration and cubicBezier tokens can now be exported as native Figma motion variables (Number/Time and Easing) — a new "Motion" checkbox in the variable export options controls the export.
