---
"@tokens-studio/figma-plugin": patch
---

Extended collections now properly import variables with override values. When importing from a collection that extends another (Brand A extends Base), variables will show the effective values including any overrides defined in the extended collection.
